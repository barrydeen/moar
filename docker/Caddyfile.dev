# Dev Caddyfile — no TLS, plain HTTP

:80 {
    reverse_proxy server:8080
}

:8888 {
    handle /api/* {
        reverse_proxy server:8080
    }
    handle {
        reverse_proxy admin:3000
    }
}
